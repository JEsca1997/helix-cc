# helix — cc (public)

helix's Klein-4 `cc` frame — public broadcast, no account required.
Phase 1 of the phased Klein-4 GitHub upload (Company.txt §3G).

Schema: `toc.md` + `Organization/Company/enterprise/c/cc/src/{b,f,m,shr}` — identical shape
mandated across every entity's cc repo. Tier: enterprise.
